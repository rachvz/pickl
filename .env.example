# Debug logging (set to specific namespace or * for all)
# DEBUG=framework:*,test:*

# Browser display mode (set to false to see browser during tests)
HEADLESS=true

# Browser type (chromium, firefox, webkit) - can be overridden by npm scripts
BROWSER=chromium

# Base URL for application under test
BASE_URL=https://the-internet.herokuapp.com

# Cucumber tag filtering (e.g., "@smoke", "not @skip", "@login and not @slow")
TAGS=not @skip

# CI environment flag (automatically set by CI/CD platforms)
# CI=true
